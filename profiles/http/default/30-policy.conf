# DoS protection belongs to the module's local layer (waf_local_rate, the ip inspector), which acts
# before the bus.
SecRuleRemoveByTag "attack-dos"

# Profile marker: shows in the audit which set actually applied.
SecAction "id:1000000,phase:1,nolog,pass,t:none,setvar:tx.modsec_profile=default"
