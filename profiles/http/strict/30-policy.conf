# Политика та же, что у default: строже здесь уровень паранойи, а не список
# выключенного. Обоснование каждой строки -- в ../default/30-policy.conf.
SecRuleRemoveByTag "attack-dos"

SecAction "id:1000000,phase:1,nolog,pass,t:none,setvar:tx.modsec_profile=strict"
