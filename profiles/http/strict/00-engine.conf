# Профиль strict отличается от default только уровнем паранойи. Настройки
# движка у них общие, и общие они буквально: файл включается, а не копируется.
#
# Это и есть смысл раскладки «подпапка на профиль» из profiles/README.md --
# профили делят одну базу CRS и расходятся в одном небольшом файле, а не держат
# по своей копии набора.

Include ../default/00-engine.conf
